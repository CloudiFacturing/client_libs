This is a Java front-end for the Generic Storage Service, implemented for the CloudFlow and CAxMan projects.

It is a Maven project, and it should therefore download all required dependencies at compile time.

It is confirmed to build in Netbeans 8 (IDE).

